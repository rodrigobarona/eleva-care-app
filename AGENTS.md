# AGENTS.md

## Learned User Preferences

- When the user says "commit and push", run `git commit --no-verify` and `git push --no-verify`; they repeatedly bypass pre-commit hooks.
- Treat user-provided plan files in `.cursor/plans/*.plan.md` as the source of truth: do not edit the plan, do not recreate the pre-existing todos, mark them `in_progress` as you work, and do not stop until every todo is complete.
- Before non-trivial library/framework work (Stripe, Next.js, Drizzle, Novu, React Email, WorkOS, Resend, Vercel Workflows, etc.), consult the Context7 MCP for current best practices instead of relying on memory.
- Use the Stripe MCP to audit live Stripe configuration (products, prices, tax, Connect accounts, fees, lookup keys) when validating payment flows; cross-check with code rather than assuming.
- Reject silent fallbacks that mask root causes (e.g., schema rollout mismatches, locale fallback to English): diagnose and fix the underlying problem rather than papering over it.
- The user writes in mixed English and Portuguese with frequent typos; interpret intent, do not request rewordings or correct spelling.
- When asked to "verify each finding", read the cited code first and only apply a fix when the finding is genuinely present in the current code.
- Default to running production builds and scripts with `pnpm` / `pnpm tsx` (never npm or yarn).
- Vision / blueprint / product docs require deep prose with personas described as day-to-day journeys, business model, explicit non-goals, and mermaid diagrams — not one-line summaries or buzzword lists.
- Treat user-stated v2 stack swaps as locked decisions (e.g., WorkOS replaces Clerk, Resend + Resend Automation replace Novu, Vercel Workflows replaces QStash, drop PostHog/Dub) — don't relitigate them in subsequent plans.
- Apply production database schema changes by handing the user an idempotent SQL script to run manually in the Neon SQL editor, plus a verification query; the user runs them and pastes back results — don't run automated migrations against the production DB.

## Learned Workspace Facts

- Stack on `main`: Next.js 16 App Router, TypeScript, Clerk auth, Stripe + Stripe Connect, Novu for transactional emails, React Email templates, Drizzle ORM on Neon Postgres, Upstash Redis, QStash for cron schedules, Tailwind + shadcn/ui. An in-flight WorkOS migration lives on origin `clerk-workos` branch — mine it (its `_docs/`, `src/lib/integrations/workos/sync.ts`, RBAC config, Vault plan, etc.) before designing new v2 patterns. On `main` the schema still keys off Clerk user IDs (`clerkId`), so database access must use the `DATABASE_URL_LEGACY` env — `DATABASE_URL` now points at the WorkOS database.
- Routing layout: private app under `app/(private)/...`, public localized pages under `app/[locale]/(public)/...` (locales include at least `pt`, `en`, `es`, and `pt-BR` mapped to file `br.json`), HTTP handlers under `app/api/...`; private app uses `ELEVA_LOCALE` cookie, public uses prefix `as-needed`.
- Canonical form component is `components/features/forms/MeetingForm.tsx`; the file at `src/components/features/forms/MeetingForm.tsx` is a stale duplicate on `main`—do not edit it (the `clerk-workos` branch already standardizes on `src/`).
- Stripe: Connect Express with a 15% platform fee; supports Credit Card and Portuguese Multibanco (8-day expiry); use automatic payment methods; collect NIF only (no billing address) for Portuguese tax; promo codes can break fee math and need explicit handling. There are two Stripe webhooks: `app/api/webhooks/stripe/route.ts` (account/snapshot events, handlers in `app/api/webhooks/stripe/handlers/`) and `app/api/webhooks/stripe-connect` (connected/v2 account events); `StripeProcessedEventTable` (`stripe_processed_events`) dedupes deliveries by inserting `event.id` before processing and skipping on conflict. PaymentIntent enrichment must run before any early-return that skips Novu notifications so dispute/refund events can derive customer data. Guest phone is captured end-to-end via `meetings.guestPhone`, `pack_purchases.buyer_phone`, and `payment_transfers.guest_phone`.
- Novu integration sits in `config/novu.ts` and `lib/integrations/novu/` (notably `email-service.ts`, `email.ts`); workflow ids include `payment-universal` and `appointment-confirmation`; subscriber payload built by `buildNovuSubscriberFromStripe`. React Email templates live in `emails/`; preserve locale labels via `emails/utils/i18n.ts` (e.g., `locale === 'pt' ? 'Data' : 'Date'`); conditionally render rows when fields are empty strings.
- Database: schema in `drizzle/schema.ts`; the `neon-http` driver does NOT support transactions—use the pooled/serverless driver (`@neondatabase/serverless`) when transactions are required, or refactor to single-statement operations.
- FormCache (Redis-backed) has a known TOCTOU race between `isProcessing` and `set`; guard with atomic SET NX or a lock when adding new write paths.
- Cron schedules are driven by QStash and call `app/api/cron/appointment-reminders` and `app/api/cron/appointment-reminders-1hr`; if 0 crons are firing, re-run the QStash setup script.
- Production domain is `eleva.care`; the primary live expert account used in debugging is Patrícia Fernandes da Mota.
- v2 architecture (locked, partly already prototyped on `clerk-workos`): **org-per-user** multi-tenancy (every expert AND patient owns one org; `org_type` ∈ `expert`/`patient`/`clinic`); WorkOS AuthKit + RBAC (89 permissions × 6 roles in `resource:action` pattern; sync MUST NEVER block authentication) + Vault (envelope encryption for PHI and Google OAuth tokens); replace Novu with Resend + Resend Automation + Resend Audiences (lite CRM); replace QStash with Vercel Workflows SDK; drop PostHog and Dub; keep Sentry/BetterStack/BotID; runtime is Bun (local) + Node 24 (Vercel) hybrid; testing is Vitest + Playwright; docs site is Fumadocs; Tailwind v4; blueprint documentation lives under `_docs/blueprint/`.
- Packs feature: an expert sells a "pack of N sessions" Stripe product via `app/api/create-pack-checkout`; the purchase (tracked in `pack_purchases`) generates a Stripe promo code emailed to the buyer that is redeemed at booking checkout; pack prices are stored in cents and the admin UI rejects whole-euro shorthand. The expert earnings dashboard lives at `app/(private)/dashboard/earnings` (Airbnb-host-inspired, Stripe widgets read-only, labels "upcoming"/"paid", payout the day after the session). Vercel BotID guards `/api/create-payment-intent` and `/api/create-pack-checkout` and returns 403 for requests missing client-side BotID protection.
- Product positioning: Eleva.care is a digital health platform — NOT a clinical provider per the ERS Portugal memo (`_docs/ERS_portugal/`); women's-health vertical with six categories (Pelvic Health, Pregnancy & Postpartum, Mental Health, Sexual Health, Hormonal Health & Menopause, Nutrition); credentialed-expert moat anchored by founders Patricia Mota, Cristine Homsi Jorge, Alexandre Delgado — keep team/credentials as first-class CMS surfaces. v2 expert subscriptions use Stripe **lookup keys** (no hardcoded price IDs); tiers are Community (20% monthly / 12% annual commission) and Top (18% / 8%).
